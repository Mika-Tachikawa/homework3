<div class="container">
<div class="row">

  <div class="col-lg-2">
  </div>

  <div class="col-lg-10">
  <h1>Editing Book</h1>

  <div>
  <% if @book.errors.any? %>
    <%= @book.errors.count %>errors prohibited this book from being saved:
    <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  <% end %>
  </div>

  <%=form_with model: @book do |f| %>

    <p class="box1"><p>Title</p></p>
    <p class="box2"><%= f.text_field :title, size: "50" %></p>

    <p class="box1"><p>Opinion</p></p>
    <p class="box2"><%= f.text_area :body, size: "50" %></p>


    <p><%= f.submit 'Update Book', class: "btn btn-success"%></p>

  <% end %>

    <%= link_to "Show", book_path(@book.id) %>
    <span>|</span>
    <a href="/books">Back</a>
  </div>

</div>
</div>